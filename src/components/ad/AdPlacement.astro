---
import IndieBoostingWrapper from "./IndieBoostingWrapper.jsx";

interface Props {
  channelId: string;
}

const { channelId } = Astro.props;
---

<IndieBoostingWrapper id={channelId} client:load/>
